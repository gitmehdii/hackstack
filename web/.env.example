# URL de l'API hackstack (côté serveur uniquement — jamais exposée au client).
# Dev local : l'API tourne via `uvicorn api.main:app`.
API_URL=http://localhost:8099

# URL publique du site, pour metadataBase et les balises Open Graph / sitemap.
SITE_URL=http://localhost:3000
