# Dépendances de l'API seule, pour le déploiement en fonction serverless (Vercel).
# Le projet est géré par uv/pyproject ; ce fichier existe parce que le runtime Python de
# Vercel lit requirements.txt, et surtout parce que le déploiement ne doit embarquer QUE
# ce que l'API importe au niveau module.
#
# Volontairement absent : sentence-transformers et torch (~2 Go). L'encodage des requêtes
# passe par l'inférence HuggingFace (EMBED_BACKEND=remote) ; `api/search_encoder.py`
# n'importe le modèle local que tardivement, dans une branche jamais prise en production.
# Les embarquer ferait exploser la limite de taille du bundle pour rien.
fastapi>=0.115
psycopg[binary]>=3.2
psycopg-pool>=3.2
httpx>=0.27
pyyaml>=6.0
